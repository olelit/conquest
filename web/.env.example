# Google OAuth: тот же Client ID, что и на сервере (docker-compose.yml -> GOOGLE_CLIENT_ID)
# Нужно создать OAuth 2.0 Client ID (тип "Web application") в Google Cloud Console
# и добавить в Authorized JavaScript origins: http://localhost:5173
VITE_GOOGLE_CLIENT_ID=
