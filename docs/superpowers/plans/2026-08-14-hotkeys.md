# Хоткеи и удобство — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Горячие клавиши: цифры 1–9/0 — армия (10%–100%), Tab — полная карта, Пробел — пауза (соло), Esc — закрытие меню/бургера; строка-подсказка.

**Architecture:** Единый `onHotkey` на window в App.vue (объединяет существующий Esc-обработчик меню); HexMap выставляет `resetView` через `defineExpose`; подсказка — фиксированная строка внизу слева.

**Tech Stack:** Vue 3 + Vite.

## Global Constraints

- Проверка web: `npm run build` из `web/`.
- Язык UI — русский.
- Спецификация: `docs/superpowers/specs/2026-08-14-hotkeys-design.md`.
- Слушатели window добавляются в onMounted и снимаются в onBeforeUnmount (именованные функции).

---

### Task 1: Хоткеи и подсказки

**Files:**
- Modify: `web/src/App.vue`, `web/src/components/HexMap.vue`

**Interfaces:**
- Produces: `onHotkey` (window keydown); `HexMap.resetView` через `defineExpose`; `hexMapRef`; подсказка.

- [ ] **Step 1: HexMap — resetView**

В `web/src/components/HexMap.vue`, после `function panStep(...)`:

```ts
function resetView(): void {
  view.value = null;
}

defineExpose({ resetView });
```

- [ ] **Step 2: App.vue — обработчик хоткеев**

В `web/src/App.vue`:

1. Заменить `onMenuKeydown` на `onHotkey`:

```ts
function onHotkey(e: KeyboardEvent): void {
  if ((e.target as HTMLElement | null)?.closest('input, select, textarea')) return;
  if (e.code === 'Escape') {
    closeContextMenu();
    burgerOpen.value = false;
    return;
  }
  if (!room.value || !game.value) return;
  if (e.code.startsWith('Digit')) {
    const digit = Number(e.code.slice(5));
    if (!Number.isNaN(digit)) {
      army.value = digit === 0 ? 100 : digit * 10;
    }
    return;
  }
  if (e.code === 'Tab') {
    e.preventDefault();
    hexMapRef.value?.resetView();
    return;
  }
  if (e.code === 'Space') {
    e.preventDefault();
    if (showPause.value) onPause();
  }
}
```

2. Рядом с `contextMenu` ref:

```ts
const hexMapRef = ref<InstanceType<typeof HexMap> | null>(null);
```

3. В `onMounted`/`onBeforeUnmount` заменить `keydown`-слушатели `onMenuKeydown` на `onHotkey`.

4. Шаблон HexMap — добавить ref:

```html
        <HexMap
          ref="hexMapRef"
          v-if="game"
          ...
```

5. Подсказка (в `.game-screen`, после FpsOverlay):

```html
        <div v-if="room && game && !room.loadTest" class="hotkeys-hint">
          1–9/0 — армия · Tab — карта · Пробел — пауза
        </div>
```

6. CSS:

```css
.hotkeys-hint {
  position: fixed;
  bottom: 16px;
  left: 16px;
  z-index: 15;
  color: #666;
  font-size: 12px;
  background: rgba(0, 0, 0, 0.55);
  border-radius: 6px;
  padding: 4px 10px;
  pointer-events: none;
  white-space: nowrap;
}
```

- [ ] **Step 3: Проверка сборки**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 4: Коммит**

```bash
git add web/src/App.vue web/src/components/HexMap.vue
git commit -m "feat: хоткеи — армия 1-9/0, Tab-карта, Пробел-пауза, Esc-меню, подсказки"
```

---

### Task 2: Финальная проверка

**Files:**
- нет изменений

- [ ] **Step 1: Сборка web**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 2: Ручная проверка**

Run: dev-серверы, затем:
1. В игре: 5 → армия 50%, 9 → 90%, 0 → 100%.
2. Tab — камера показывает всю карту.
3. Пробел — пауза (соло), повторно — продолжить.
4. Esc — закрывает ПКМ-меню и бургер.
5. Ввод цифр в поле армии — хоткеи не срабатывают.
6. В режиме нагрузочного теста подсказки нет.

- [ ] **Step 3: Коммит (если были правки)**

```bash
git add -A
git commit -m "fix: правки по итогам ручной проверки"
```
