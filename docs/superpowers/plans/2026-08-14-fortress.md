# Крепость — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Постройка «крепость» на своём гексе: защита гекса и соседей (дренаж атаки +25%), лимит построек `floor(клетки/15)`, −100 к лимиту очков за крепость, снос через ПКМ, иконка на карте, уничтожение при смене владельца.

**Architecture:** `HexState.fortress: boolean`; `pointLimit(count, fortresses)`; валидации `validateBuildFortress`/`validateRemoveFortress`; эффект — в `tickBattles` через `isFortressProtected`; сообщения `build-fortress`/`remove-fortress`; web — иконка ⚑, пункты меню.

**Tech Stack:** Node.js + TypeScript + vitest (server); Vue 3 + Vite (web).

## Global Constraints

- Тесты сервера: `npx vitest run` из `server/` (179 существующих тестов зелёные).
- Проверка web: `npm run build` из `web/`.
- Язык логов/сообщений — русский.
- Спецификация: `docs/superpowers/specs/2026-08-14-fortress-design.md`.
- `pointLimit` — сигнатура меняется на `(count, fortresses = 0)`; все вызовы обновить.

---

### Task 1: Правила — данные, лимиты, постройка/снос, уничтожение

**Files:**
- Modify: `server/src/rules.ts`
- Test: `server/test/rules.test.ts`

**Interfaces:**
- Produces: `HexState.fortress: boolean`; `fortressLimit(hexCount): number`; `fortressCount(state, playerId): number`; `pointLimit(count, fortresses = 0): number`; `validateBuildFortress(state, playerId, q, r)`; `buildFortress(state, playerId, q, r)`; `validateRemoveFortress(state, playerId, q, r)`; `removeFortress(state, playerId, q, r)`; уничтожение флага при смене владельца во всех путях.

- [ ] **Step 1: Написать падающие тесты**

В `server/test/rules.test.ts` добавить:

```ts
describe('крепость', () => {
  it('лимит построек: floor(клетки/15)', () => {
    expect(fortressLimit(14)).toBe(0);
    expect(fortressLimit(15)).toBe(1);
    expect(fortressLimit(30)).toBe(2);
    expect(fortressLimit(45)).toBe(3);
  });
  it('лимит очков уменьшается на 100 за крепость', () => {
    expect(pointLimit(10)).toBe(1500);
    expect(pointLimit(10, 1)).toBe(1400);
    expect(pointLimit(10, 5)).toBe(1000);
    expect(pointLimit(5, 10)).toBe(0); // не ниже 0
  });
  it('постройка: свой гекс, не в битве, лимит построек, запас очков', () => {
    const s = makeState([{ q: 5, r: 5, ownerId: P }]);
    s.players[0].capital = { q: 5, r: 5 };
    // 1 клетка — лимит 0
    expect(validateBuildFortress(s, P, 5, 5).ok).toBe(false);
    // 15 клеток — можно, очки 1000 <= новый лимит 2000-100=1900? нет: лимит = 1000+15*50-100 = 1650 >= 1000
    for (let i = 1; i < 15; i++) s.hexes[i].ownerId = P;
    expect(validateBuildFortress(s, P, 5, 5).ok).toBe(true);
    buildFortress(s, P, 5, 5);
    expect(s.hexes.find((h) => h.q === 5 && h.r === 5)!.fortress).toBe(true);
    expect(fortressCount(s, P)).toBe(1);
  });
  it('постройка: нельзя на чужом гексе и при нехватке очков-лимита', () => {
    const s = makeState([{ q: 5, r: 5, ownerId: AI }, { q: 6, r: 5, ownerId: P }]);
    expect(validateBuildFortress(s, P, 5, 5).ok).toBe(false); // чужой
    for (let i = 0; i < 15; i++) s.hexes[i].ownerId = P;
    const player = s.players[0];
    player.points = 2000; // новый лимит с крепостью = 1000+750-100 = 1650 < 2000
    expect(validateBuildFortress(s, P, 6, 5).ok).toBe(false);
  });
  it('постройка: нельзя на гексе в битве', () => {
    const s = makeState([{ q: 5, r: 5, ownerId: P, attackerId: AI, attackInvestment: 100 }]);
    for (let i = 1; i < 15; i++) s.hexes[i].ownerId = P;
    expect(validateBuildFortress(s, P, 5, 5).ok).toBe(false);
  });
  it('снос крепости', () => {
    const s = makeState([{ q: 5, r: 5, ownerId: P, fortress: true }]);
    expect(validateRemoveFortress(s, P, 5, 5).ok).toBe(true);
    expect(validateRemoveFortress(s, P, 6, 5).ok).toBe(false);
    removeFortress(s, P, 5, 5);
    expect(s.hexes.find((h) => h.q === 5 && h.r === 5)!.fortress).toBe(false);
  });
  it('крепость уничтожается при захвате в битве', () => {
    const s = makeState([
      { q: 5, r: 5, ownerId: P, fortress: true, attackerId: AI, defenderId: P, attackInvestment: 500, battleProgress: CAPTURE_TICKS - 1 },
      { q: 6, r: 5, ownerId: AI },
    ]);
    tickBattles(s);
    const hex = s.hexes.find((h) => h.q === 5 && h.r === 5)!;
    expect(hex.ownerId).toBe(AI);
    expect(hex.fortress).toBe(false);
  });
  it('крепость уничтожается при отрезании', () => {
    const s = makeState([
      { q: 2, r: 2, ownerId: P }, { q: 3, r: 2, ownerId: P },
      { q: 5, r: 5, ownerId: P, fortress: true },
    ]);
    s.players[0].capital = { q: 3, r: 2 };
    s.hexes.find((h) => h.q === 2 && h.r === 2)!.ownerId = null; // шейка захвачена
    applyCut(s, P);
    expect(s.hexes.find((h) => h.q === 5 && h.r === 5)!.ownerId).toBeNull();
    expect(s.hexes.find((h) => h.q === 5 && h.r === 5)!.fortress).toBe(false);
  });
  it('крепость уничтожается при окружении', () => {
    const s = makeState([{ q: 7, r: 6, ownerId: P, fortress: true }, { q: 8, r: 6, ownerId: P }]);
    s.players[0].capital = { q: 7, r: 6 };
    for (const [q, r] of [[6,6],[7,7],[7,5],[8,5],[6,7],[9,6],[8,7],[9,5]]) {
      s.hexes.find((h) => h.q === q && h.r === r)!.ownerId = AI;
    }
    rules.declareWar(s, P, AI);
    applyEnclosure(s);
    expect(s.hexes.find((h) => h.q === 7 && h.r === 6)!.fortress).toBe(false);
  });
});
```

(в начало файла добавить импорты: `fortressLimit, fortressCount, buildFortress, removeFortress, validateBuildFortress, validateRemoveFortress`.)

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `npx vitest run test/rules.test.ts -t "крепость"`
Expected: FAIL — `fortressLimit is not defined`.

- [ ] **Step 3: Реализовать**

В `server/src/rules.ts`:

1. В `HexState` (после `battleProgress`) добавить:

```ts
  fortress: boolean;
```

2. `pointLimit` заменить:

```ts
export function pointLimit(hexCount: number, fortresses = 0): number {
  return Math.max(0, BASE_POINTS + hexCount * LIMIT_PER_HEX - 100 * fortresses);
}
```

3. Добавить (после `terrainCost`):

```ts
export function fortressLimit(hexCount: number): number {
  return Math.floor(hexCount / 15);
}

export function fortressCount(state: GameState, playerId: number): number {
  return state.hexes.reduce((n, h) => n + (h.ownerId === playerId && h.fortress ? 1 : 0), 0);
}
```

4. Валидации и действия (после `validateDefend`):

```ts
export function validateBuildFortress(state: GameState, playerId: number, q: number, r: number): ActionValidation {
  if (hasGameWinner(state)) return { ok: false, error: 'Игра окончена' };
  const hex = findHex(state, q, r);
  if (!hex) return { ok: false, error: 'Гекс не найден' };
  if (hex.ownerId !== playerId) return { ok: false, error: 'Это не ваш гекс' };
  if (hex.attackerId !== null) return { ok: false, error: 'За гекс идёт битва' };
  if (hex.fortress) return { ok: false, error: 'Здесь уже есть крепость' };
  const player = state.players.find((p) => p.id === playerId);
  if (!player || player.eliminated) return { ok: false, error: 'Вы выбыли из игры' };
  const count = hexCount(state, playerId);
  if (fortressCount(state, playerId) >= fortressLimit(count)) {
    return { ok: false, error: 'Достигнут лимит крепостей' };
  }
  if (player.points > pointLimit(count, fortressCount(state, playerId) + 1)) {
    return { ok: false, error: 'Сначала потратьте очки: крепость уменьшает лимит' };
  }
  return { ok: true };
}

export function buildFortress(state: GameState, playerId: number, q: number, r: number): void {
  const hex = findHex(state, q, r)!;
  hex.fortress = true;
}

export function validateRemoveFortress(state: GameState, playerId: number, q: number, r: number): ActionValidation {
  if (hasGameWinner(state)) return { ok: false, error: 'Игра окончена' };
  const hex = findHex(state, q, r);
  if (!hex) return { ok: false, error: 'Гекс не найден' };
  if (hex.ownerId !== playerId || !hex.fortress) return { ok: false, error: 'Здесь нет вашей крепости' };
  return { ok: true };
}

export function removeFortress(state: GameState, playerId: number, q: number, r: number): void {
  const hex = findHex(state, q, r)!;
  hex.fortress = false;
}
```

5. Уничтожение при смене владельца — добавить `hex.fortress = false;` в следующих местах:
   - `applyCapture` (строка `hex.ownerId = playerId;` — сразу после);
   - `tickBattles` обе ветки (`hex.ownerId = hex.attackerId;` и `hex.ownerId = hex.defenderId;` — после);
   - `applyEnclosure` (`for (const hex of region) hex.ownerId = owner;` — в цикле);
   - `applyCut` (`hex.ownerId = null;` — в цикле);
   - `eliminateIfCapitalLost` (оба места: `owned[i].ownerId = null;` и `for (const hex of ai.hexes) hex.ownerId = ai.id;`).

6. `applyIncome` — передать крепости:

```ts
    player.points = Math.min(player.points + income, pointLimit(count, fortressCount(state, player.id)));
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run test/rules.test.ts -t "крепость"` → PASS. Затем `npx vitest run` — все зелёные.

- [ ] **Step 5: Коммит**

```bash
git add server/src/rules.ts server/test/rules.test.ts
git commit -m "feat: крепость — лимиты, постройка/снос, уничтожение при смене владельца"
```

---

### Task 2: Правила — эффект в битве (дренаж атаки +25%)

**Files:**
- Modify: `server/src/rules.ts`
- Test: `server/test/rules.test.ts`

**Interfaces:**
- Consumes: `HexState.fortress` (Task 1).
- Produces: `FORTRESS_DRAIN_PER_TICK = 13`; `isFortressProtected(state, hex): boolean`; `tickBattles` применяет увеличенный дренаж атакующему на защищённых гексах.

- [ ] **Step 1: Написать падающий тест**

В `server/test/rules.test.ts` (блок «крепость»):

```ts
  it('защищённый гекс: атакующий теряет 13 очков/тик вместо 10', () => {
    const s = makeState([
      { q: 5, r: 5, ownerId: P, fortress: true },
      { q: 6, r: 5, ownerId: P },
      { q: 7, r: 5, ownerId: AI, attackerId: AI, defenderId: P, attackInvestment: 100, defenseInvestment: 0 },
    ]);
    tickBattles(s);
    const hex = s.hexes.find((h) => h.q === 7 && h.r === 5)!;
    expect(hex.attackInvestment).toBe(100 - 13);
  });
  it('незащищённый гекс: дренаж 10', () => {
    const s = makeState([
      { q: 5, r: 5, ownerId: P },
      { q: 6, r: 5, ownerId: P },
      { q: 7, r: 5, ownerId: AI, attackerId: AI, defenderId: P, attackInvestment: 100, defenseInvestment: 0 },
    ]);
    tickBattles(s);
    const hex = s.hexes.find((h) => h.q === 7 && h.r === 5)!;
    expect(hex.attackInvestment).toBe(90);
  });
  it('крепость на самом атакуемом гексе тоже защищает', () => {
    const s = makeState([
      { q: 5, r: 5, ownerId: P, fortress: true, attackerId: AI, defenderId: P, attackInvestment: 100, defenseInvestment: 0 },
    ]);
    tickBattles(s);
    const hex = s.hexes.find((h) => h.q === 5 && h.r === 5)!;
    expect(hex.attackInvestment).toBe(87);
  });
```

Примечание: гекс (6,5) смежен с (5,5) и (7,5) — соседняя крепость (5,5) защищает битву за (7,5). Дренаж 100→87 во втором тесте с крепостью на самом гексе: 13 — но битва на (5,5) сама защищена.

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `npx vitest run test/rules.test.ts -t "защищённый гекс"`
Expected: FAIL — дренаж 10.

- [ ] **Step 3: Реализовать**

В `server/src/rules.ts`:

1. Рядом с `DRAIN_PER_TICK`-импортами (после констант) добавить:

```ts
export const FORTRESS_DRAIN_PER_TICK = Math.round(DRAIN_PER_TICK * 1.25);
```

2. Добавить (после `fortressCount`):

```ts
export function isFortressProtected(state: GameState, hex: HexState): boolean {
  if (hex.fortress) return true;
  const defender = hex.defenderId ?? hex.ownerId;
  if (defender === null) return false;
  return NEIGHBOR_OFFSETS.some(([dq, dr]) => {
    const nq = hex.q + dq;
    const nr = hex.r + dr;
    if (!isInBounds(state, nq, nr)) return false;
    const neighbor = findHex(state, nq, nr);
    return neighbor !== undefined && neighbor.fortress && neighbor.ownerId === defender;
  });
}
```

3. В `tickBattles` — заменить строку дренажа атаки:

```ts
    hex.attackInvestment = Math.max(0, hex.attackInvestment - DRAIN_PER_TICK);
```

на:

```ts
    const attackerDrain = isFortressProtected(state, hex) ? FORTRESS_DRAIN_PER_TICK : DRAIN_PER_TICK;
    hex.attackInvestment = Math.max(0, hex.attackInvestment - attackerDrain);
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run test/rules.test.ts -t "крепость"` → PASS. Затем `npx vitest run` — все зелёные.

- [ ] **Step 5: Коммит**

```bash
git add server/src/rules.ts server/test/rules.test.ts
git commit -m "feat: крепость защищает гекс и соседей — дренаж атаки 13 вместо 10"
```

---

### Task 3: Комната — build/remove-fortress и лимит в view

**Files:**
- Modify: `server/src/rooms.ts`, `server/src/ws.ts`
- Test: `server/test/rooms.test.ts`

**Interfaces:**
- Consumes: `validateBuildFortress/buildFortress/validateRemoveFortress/removeFortress` (Task 1).
- Produces: сообщения `build-fortress`/`remove-fortress`; `view()` передаёт лимит с учётом крепостей.

- [ ] **Step 1: Написать падающий тест**

В `server/test/rooms.test.ts` добавить (блок «Room: крепость»):

```ts
describe('Room: крепость', () => {
  it('build-fortress и remove-fortress через handleAction', () => {
    const room = new Room(1, 'Тест', 'normal', 6, true, 1, () => 0.5);
    room.addHuman('A', 1);
    room.start(1);
    const g = room.gameState!;
    for (let i = 0; i < 15; i++) g.hexes[i].ownerId = 1;
    g.players[0].capital = { q: g.hexes[0].q, r: g.hexes[0].r };
    const hex = g.hexes[0];
    const build = room.handleAction(1, 'build-fortress', { q: hex.q, r: hex.r });
    expect(build.type).toBe('state');
    expect(hex.fortress).toBe(true);
    expect(room.view(1).log.some((l) => l.includes('построил крепость'))).toBe(true);
    const remove = room.handleAction(1, 'remove-fortress', { q: hex.q, r: hex.r });
    expect(remove.type).toBe('state');
    expect(hex.fortress).toBe(false);
    expect(room.view(1).log.some((l) => l.includes('снёс крепость'))).toBe(true);
  });
  it('view: лимит уменьшен на 100 за крепость', () => {
    const room = new Room(1, 'Тест', 'normal', 6, true, 1, () => 0.5);
    room.addHuman('A', 1);
    room.start(1);
    const g = room.gameState!;
    for (let i = 0; i < 15; i++) g.hexes[i].ownerId = 1;
    g.players[0].capital = { q: g.hexes[0].q, r: g.hexes[0].r };
    const before = room.view(1).game!.players[0].limit;
    g.hexes[0].fortress = true;
    const after = room.view(1).game!.players[0].limit;
    expect(after).toBe(before! - 100);
  });
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `npx vitest run test/rooms.test.ts -t "крепость"`
Expected: FAIL — «Неизвестный тип сообщения: build-fortress».

- [ ] **Step 3: Реализовать**

В `server/src/rooms.ts`:

1. В `handleAction` — новые case (после `defend`):

```ts
      case 'build-fortress': {
        validation = rules.validateBuildFortress(this.state, playerId, msg.q, msg.r);
        if (!validation.ok) return { type: 'error', message: validation.error };
        rules.buildFortress(this.state, playerId, msg.q, msg.r);
        this.addLog(`${this.playerName(playerId)} построил крепость на (${msg.q}, ${msg.r})`);
        return { type: 'state' };
      }
      case 'remove-fortress': {
        validation = rules.validateRemoveFortress(this.state, playerId, msg.q, msg.r);
        if (!validation.ok) return { type: 'error', message: validation.error };
        rules.removeFortress(this.state, playerId, msg.q, msg.r);
        this.addLog(`${this.playerName(playerId)} снёс крепость на (${msg.q}, ${msg.r})`);
        return { type: 'state' };
      }
```

2. В `view()` — лимит с крепостями:

```ts
                limit: hidden ? null : rules.pointLimit(rules.hexCount(state, p.id), rules.fortressCount(state, p.id)),
```

В `server/src/ws.ts`:

1. Новые case (после `'respond-proposal'`):

```ts
          case 'build-fortress':
          case 'remove-fortress': {
            const result = manager.handleAction(connId, message as { type: string; q?: number; r?: number });
            if (result.type === 'state') broadcast();
            else ws.send(JSON.stringify(result));
            return;
          }
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run test/rooms.test.ts -t "крепость"` → PASS. Затем `npx vitest run` — все зелёные.

- [ ] **Step 5: Коммит**

```bash
git add server/src/rooms.ts server/src/ws.ts server/test/rooms.test.ts
git commit -m "feat: build/remove-fortress в комнате, лимит очков с крепостями в view"
```

---

### Task 4: Web — меню, иконка, тултип

**Files:**
- Modify: `web/src/types.ts`, `web/src/api.ts`, `web/src/components/ContextMenu.vue`, `web/src/components/HexMap.vue`

**Interfaces:**
- Consumes: `HexState.fortress` в payload (Task 1).
- Produces: `Hex.fortress?: boolean`; `sendBuildFortress(q, r)`/`sendRemoveFortress(q, r)`; пункты меню «Построить крепость»/«Снести крепость»; иконка ⚑ и строка тултипа.

- [ ] **Step 1: Типы и API**

В `web/src/types.ts`:

1. `Hex` — добавить `fortress?: boolean;`
2. `ClientMessage` — добавить:

```ts
  | { type: 'build-fortress'; q: number; r: number }
  | { type: 'remove-fortress'; q: number; r: number }
```

В `web/src/api.ts`:

```ts
  sendBuildFortress(q: number, r: number): void {
    this.send({ type: 'build-fortress', q, r });
  }

  sendRemoveFortress(q: number, r: number): void {
    this.send({ type: 'remove-fortress', q, r });
  }
```

- [ ] **Step 2: ContextMenu — пункты крепости**

В `web/src/components/ContextMenu.vue`:

1. Props добавить:

```ts
  hexCount: number;
  fortressCount: number;
  points: number;
```

2. Emits добавить:

```ts
  buildFortress: [];
  removeFortress: [];
```

3. Пункты для своего гекса:

```ts
const isMine = computed(() => props.hex.ownerId !== null && props.hex.ownerId === props.humanId);

const canBuildFortress = computed(() => {
  const limit = Math.floor(props.hexCount / 15);
  const nextLimit = 1000 + props.hexCount * 50 - 100 * (props.fortressCount + 1);
  return limit > props.fortressCount && props.points <= nextLimit;
});

const items = computed(() => {
  if (isMine.value) {
    if (props.hex.fortress) {
      return [{ label: 'Снести крепость', disabled: false, hint: '', action: 'remove-fortress' }];
    }
    return [
      { label: 'Построить крепость', disabled: !canBuildFortress.value, hint: canBuildFortress.value ? '' : 'нужно 15+ клеток и запас лимита', action: 'build-fortress' },
    ];
  }
  ... (дипломатические пункты как сейчас)
});
```

4. В `pick`:

```ts
  else if (action === 'build-fortress') emit('buildFortress');
  else if (action === 'remove-fortress') emit('removeFortress');
```

- [ ] **Step 3: App.vue — передача данных и обработчики**

В `web/src/App.vue`:

1. Обработчики:

```ts
function onMenuBuildFortress(): void {
  if (!contextMenu.value) return;
  client.sendBuildFortress(contextMenu.value.hex.q, contextMenu.value.hex.r);
  closeContextMenu();
}

function onMenuRemoveFortress(): void {
  if (!contextMenu.value) return;
  client.sendRemoveFortress(contextMenu.value.hex.q, contextMenu.value.hex.r);
  closeContextMenu();
}
```

2. Шаблон ContextMenu — добавить:

```html
          :hex-count="myPlayer?.hexCount ?? 0"
          :fortress-count="fortressCountOf(myPlayer?.id)"
          :points="myPlayer?.points ?? 0"
          @build-fortress="onMenuBuildFortress"
          @remove-fortress="onMenuRemoveFortress"
```

3. Хелпер:

```ts
function fortressCountOf(playerId: number | null): number {
  if (playerId === null || !game.value) return 0;
  return game.value.hexes.filter((h) => h.ownerId === playerId && h.fortress).length;
}
```

- [ ] **Step 4: HexMap — иконка и тултип**

В `web/src/components/HexMap.vue`:

1. В hex-group, после маркера столицы:

```html
        <text
          v-if="hex.fortress"
          :x="hexCenter(hex.q, hex.r).x"
          :y="hexCenter(hex.q, hex.r).y - 11"
          text-anchor="middle"
          class="hex-fortress"
        >⚑</text>
```

2. CSS:

```css
.hex-fortress {
  font-size: 24px;
  fill: #ffb74d;
  stroke: #1a1a1a;
  stroke-width: 1.5;
  pointer-events: none;
}
```

3. Тултип — строка:

```html
      <div v-if="hovered.fortress" class="battle-tooltip__row">
        <span>Крепость</span>
      </div>
```

- [ ] **Step 5: Проверка сборки**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 6: Коммит**

```bash
git add web/src/types.ts web/src/api.ts web/src/components/ContextMenu.vue web/src/components/HexMap.vue web/src/App.vue
git commit -m "feat: web — крепость: меню, иконка, тултип"
```

---

### Task 5: Финальная проверка

**Files:**
- нет изменений

- [ ] **Step 1: Все тесты сервера**

Run: `npx vitest run` (в `server/`)
Expected: все зелёные (179 + новые ≈ 192).

- [ ] **Step 2: Сборка web**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 3: Ручная проверка**

Run: dev-серверы, затем:
1. Захватить 15+ клеток, ПКМ по своему гексу — «Построить крепость» активна; построить — иконка ⚑, лимит очков в HUD уменьшился на 100.
2. ПКМ по крепости — «Снести крепость»; лимит вернулся.
3. Атака ИИ на соседний гекс крепости — дренаж 13 (видно по тултипу битвы).
4. Захват гекса с крепостью — крепость исчезает.

- [ ] **Step 4: Коммит (если были правки)**

```bash
git add -A
git commit -m "fix: правки по итогам ручной проверки"
```
