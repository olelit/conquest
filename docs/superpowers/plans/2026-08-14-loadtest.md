# Нагрузочный тест — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Режим нагрузочного теста: комната на карте `round` с 5/10/20/30 игроками (1 человек-зритель + ИИ), FPS-оверлей с итогом, HUD/лог скрыты.

**Architecture:** `RoomManager.createLoadTest` создаёт обычную соло-комнату с `maxPlayers = aiCount + 1` (лимит 6 обходится естественно), `Room.loadTest` флаг в конструкторе и view; клиент — экран «Нагрузочный тест» в меню + `FpsOverlay.vue` (rAF-счётчик, мин/средний FPS, длительность).

**Tech Stack:** Node.js + TypeScript + vitest (server); Vue 3 + Vite (web).

## Global Constraints

- Тесты сервера: `npx vitest run` из `server/` (195 существующих тестов зелёные).
- Проверка web: `npm run build` из `web/`.
- Язык UI — русский.
- Спецификация: `docs/superpowers/specs/2026-08-14-loadtest-design.md`.
- Конструктор `Room`: новый 9-й параметр `loadTest = false` (после `difficulty`), порядок существующих не менять.

---

### Task 1: Сервер — createLoadTest

**Files:**
- Modify: `server/src/rooms.ts`, `server/src/ws.ts`
- Test: `server/test/rooms.test.ts`

**Interfaces:**
- Produces: `RoomManager.createLoadTest(connId, aiCount)`; `Room.loadTest: boolean` (9-й параметр конструктора); `RoomView.loadTest`; ws `start-load-test`.

- [ ] **Step 1: Написать падающие тесты**

В `server/test/rooms.test.ts` добавить:

```ts
describe('RoomManager: нагрузочный тест', () => {
  it('createLoadTest: комната с нужным числом ИИ и флагом loadTest', () => {
    const m = new RoomManager();
    expect(m.createLoadTest(1, 30).ok).toBe(true);
    const room = m.roomForConn(1)!;
    expect(room.view(1).loadTest).toBe(true);
    expect(room.gameState!.players).toHaveLength(31);
    expect(room.gameState!.players.filter((p) => p.isAi)).toHaveLength(30);
  });
  it('createLoadTest: недопустимое число игроков — ошибка', () => {
    const m = new RoomManager();
    expect(m.createLoadTest(1, 7).ok).toBe(false);
    expect(m.createLoadTest(1, 100).ok).toBe(false);
  });
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `npx vitest run test/rooms.test.ts -t "нагрузочный тест"`
Expected: FAIL — `createLoadTest is not a function`.

- [ ] **Step 3: Реализовать**

В `server/src/rooms.ts`:

1. Конструктор Room — добавить 9-й параметр:

```ts
  constructor(
    readonly id: number,
    readonly name: string,
    readonly mapType: MapType,
    readonly maxPlayers: number,
    readonly aiMode: boolean,
    private readonly aiCount: number,
    private readonly rng: () => number = Math.random,
    readonly difficulty: Difficulty = 'medium',
    readonly loadTest = false,
  ) {}
```

2. В `view()` (в объекте RoomView) добавить:

```ts
      loadTest: this.loadTest,
```

3. В интерфейс `RoomView` (там, где `aiMode`) добавить `loadTest: boolean;`.

4. В `RoomManager` (после `createSolo`) добавить:

```ts
  createLoadTest(connId: number, aiCount: number): { ok: true } | { ok: false; error: string } {
    if (this.connToRoom.has(connId)) return { ok: false, error: 'Вы уже в комнате' };
    if (![5, 10, 20, 30].includes(aiCount)) {
      return { ok: false, error: 'Количество игроков должно быть 5, 10, 20 или 30' };
    }
    const room = new Room(this.nextRoomId++, randomCountryName(), 'round', aiCount + 1, true, aiCount, undefined, 'medium', true);
    const slot = room.addHuman(this.connName(connId), connId);
    if (slot === null) return { ok: false, error: 'Комната заполнена' };
    this.rooms.set(room.id, room);
    this.connToRoom.set(connId, room.id);
    return room.start(connId);
  }
```

В `server/src/ws.ts` — новый case (после `'start-solo'`):

```ts
          case 'start-load-test': {
            const result = manager.createLoadTest(connId, Number(message.aiCount));
            if (result.ok) broadcast();
            else sendError(ws, result.error);
            return;
          }
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npx vitest run test/rooms.test.ts -t "нагрузочный тест"` → PASS. Затем `npx vitest run` — все зелёные.

- [ ] **Step 5: Коммит**

```bash
git add server/src/rooms.ts server/src/ws.ts server/test/rooms.test.ts
git commit -m "feat: нагрузочный тест — комната с N ИИ на карте round"
```

---

### Task 2: Web — меню, FPS-оверлей, скрытие HUD

**Files:**
- Modify: `web/src/types.ts`, `web/src/api.ts`, `web/src/App.vue`
- Create: `web/src/components/FpsOverlay.vue`

**Interfaces:**
- Consumes: `RoomView.loadTest` (Task 1).
- Produces: `sendStartLoadTest(aiCount)`; экран «Нагрузочный тест» в меню; `FpsOverlay.vue`; в тестовой комнате скрыты Hud/ArmyBar/log-panel.

- [ ] **Step 1: Типы и API**

В `web/src/types.ts`:

1. `RoomView` — добавить `loadTest: boolean;` (после `aiMode`).
2. `ClientMessage` — добавить:

```ts
  | { type: 'start-load-test'; aiCount: number }
```

В `web/src/api.ts`:

```ts
  sendStartLoadTest(aiCount: number): void {
    this.send({ type: 'start-load-test', aiCount });
  }
```

- [ ] **Step 2: FpsOverlay.vue**

Создать `web/src/components/FpsOverlay.vue`:

```vue
<script setup lang="ts">
import { onBeforeUnmount, onMounted, ref } from 'vue';

const fps = ref(0);
const minFps = ref(Infinity);
const sumFps = ref(0);
const samples = ref(0);
const startedAt = ref(Date.now());
const showSummary = ref(false);
let raf = 0;
let frames = 0;
let last = 0;
let lastUpdate = 0;

function loop(now: number): void {
  frames++;
  if (last) {
    const dt = now - last;
    if (dt > 0) {
      const current = 1000 / dt;
      if (now - lastUpdate > 500) {
        fps.value = Math.round(current);
        minFps.value = Math.min(minFps.value, current);
        sumFps.value += current;
        samples.value++;
        lastUpdate = now;
      }
    }
  }
  last = now;
  raf = requestAnimationFrame(loop);
}

function avgFps(): number {
  return samples.value ? Math.round(sumFps.value / samples.value) : 0;
}

function duration(): number {
  return Math.round((Date.now() - startedAt.value) / 1000);
}

onMounted(() => {
  raf = requestAnimationFrame(loop);
});

onBeforeUnmount(() => cancelAnimationFrame(raf));
</script>

<template>
  <div class="fps-overlay">
    <div class="fps-overlay__live">FPS: {{ fps }}</div>
    <button class="fps-overlay__btn" @click="showSummary = !showSummary">
      {{ showSummary ? 'Скрыть итог' : 'Итог' }}
    </button>
    <div v-if="showSummary" class="fps-overlay__summary">
      <div>Мин: {{ Math.round(minFps) }}</div>
      <div>Средний: {{ avgFps() }}</div>
      <div>Время: {{ duration() }} с</div>
    </div>
  </div>
</template>

<style scoped>
.fps-overlay {
  position: fixed;
  top: 16px;
  left: 16px;
  z-index: 25;
  display: flex;
  flex-direction: column;
  gap: 6px;
  background: rgba(20, 20, 26, 0.9);
  border: 1px solid #555;
  border-radius: 8px;
  padding: 10px 14px;
  color: #fff;
  font-size: 14px;
}

.fps-overlay__live {
  font-weight: 700;
  font-size: 20px;
}

.fps-overlay__btn {
  padding: 4px 10px;
  border: 1px solid #555;
  border-radius: 6px;
  background: #2a2a31;
  color: #fff;
  cursor: pointer;
}

.fps-overlay__btn:hover {
  background: #3a3a44;
}

.fps-overlay__summary {
  display: flex;
  flex-direction: column;
  gap: 2px;
  color: #ccc;
}
</style>
```

- [ ] **Step 3: App.vue — меню и шаблон игры**

В `web/src/App.vue`:

1. Импорт: `import FpsOverlay from './components/FpsOverlay.vue';`

2. `screen` тип — добавить `'loadtest'` в union и `const loadTestPlayers = ref(10);` (после `createMaxPlayers`).

3. Функции (после `goToLobby`):

```ts
function goToLoadTest(): void {
  screen.value = 'loadtest';
}

function startLoadTest(): void {
  client.sendStartLoadTest(loadTestPlayers.value);
}
```

4. В главном меню (после кнопки «Играть с людьми»):

```html
        <button class="menu__btn" :disabled="!connected" @click="goToLoadTest">Нагрузочный тест</button>
```

5. Новый экран (после блока `screen === 'lobby'`):

```html
    <template v-else-if="screen === 'loadtest' && !room">
      <div class="menu">
        <h1 class="menu__title">Conquest</h1>
        <p class="menu__subtitle">Нагрузочный тест — N игроков на карте «Круглая»</p>
        <div class="menu__row">
          <span class="menu__label">Игроков:</span>
          <select v-model.number="loadTestPlayers" class="menu__select">
            <option :value="5">5</option>
            <option :value="10">10</option>
            <option :value="20">20</option>
            <option :value="30">30</option>
          </select>
        </div>
        <button class="menu__btn" :disabled="!connected" @click="startLoadTest">Запустить тест</button>
        <button class="menu__btn menu__btn--ghost" @click="goToMenu">В меню</button>
      </div>
    </template>
```

6. В игровом экране — скрыть HUD/ArmyBar/лог в тестовом режиме и показать оверлей:

```html
        <Hud v-if="game && !room.loadTest" :game="game" :human-id="playerId" :army="army" />
```
```html
        <ArmyBar v-if="game && !room.loadTest" :game="game" :human-id="playerId" :army="army" @army-change="onArmyChange" />
```
```html
        <div v-if="room.log?.length && !room.loadTest" class="log-panel">
```
и после HexMap (внутри `.game-screen`):

```html
        <FpsOverlay v-if="room.loadTest" />
```

- [ ] **Step 4: Проверка сборки**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 5: Коммит**

```bash
git add web/src/types.ts web/src/api.ts web/src/App.vue web/src/components/FpsOverlay.vue
git commit -m "feat: web — нагрузочный тест: меню, FPS-оверлей, скрытие HUD"
```

---

### Task 3: Финальная проверка

**Files:**
- нет изменений

- [ ] **Step 1: Все тесты сервера**

Run: `npx vitest run` (в `server/`)
Expected: все зелёные (195 + 2 новых).

- [ ] **Step 2: Сборка web**

Run: `npm run build` (в `web/`)
Expected: сборка проходит.

- [ ] **Step 3: Ручная проверка**

Run: dev-серверы, затем:
1. Главное меню → «Нагрузочный тест» → 5/10/20/30.
2. Комната создаётся на карте «Круглая», HUD/лог скрыты, оверлей FPS в левом верхнем углу.
3. «Итог» — мин/средний FPS и длительность.
4. Сравнить FPS при 5 и 30 игроках.

- [ ] **Step 4: Коммит (если были правки)**

```bash
git add -A
git commit -m "fix: правки по итогам ручной проверки"
```
